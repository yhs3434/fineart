'use client'

import React from 'react'
import styles from './UploadForm.module.scss'


type UploadFormProps = {
    //
}

export default function UploadForm(_props: UploadFormProps) {
    return (
        <div className={styles.container}></div>
    )
}